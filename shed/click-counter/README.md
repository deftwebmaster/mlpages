# Click Counter Website Files

Upload the contents of this folder to the public product-page location, for example:

```text
MattLivingston.com/shed/click-counter/
```

The download button points to:

```text
downloads/click-counter-1.0.0.zip
```

Keep that ZIP in the `downloads` folder unless you update the links in `index.html`.
